// -------------------------state-----------------------------------------------------------
// moved to state.test.js file

// -------------------------mutations-----------------------------------------------------------

//--------------------------getters----------------------------------------------------------

// -------------------------action-----------------------------------------------------------

describe(" store index", () => {
  it("moved all test to separate files", () => {
    expect(1 + 1).toBe(2);
  });
});
